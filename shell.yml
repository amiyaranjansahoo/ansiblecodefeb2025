- name: Understanding the command module
  hosts: apache
  become: yes
  
  tasks:
    - name: command module
      shell: uptime | cut -d" " -f5
      register: result
    - name: Display using debug module
      debug: 
        var: result.stdout
