- name: Gather facts
  hosts: apache
  become: yes

  tasks:
    - name: gather facts
      debug:
        var: ansible_facts.all_ipv4_addresses
    - name: gather facts
      debug:
        var: ansible_facts.all_ipv6_addresses
